import * as types from './constant'

export default function (state = {}, action) {
    switch (action.type) {
        case 'condition':
            return state
        default:
            return state
    }
}
